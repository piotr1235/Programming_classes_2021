package kdruc.snake;

import java.awt.*;

public enum Color1 {
		White(Color.WHITE),
		Black(Color.BLACK),
		Hmm(new Color(30,30,50));

		private Color color;

		Color1(Color color) {
				this.color = color;

		}
}
